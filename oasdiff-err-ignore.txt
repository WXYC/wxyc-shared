# oasdiff error-level ignore list.
#
# Format: one line per suppressed finding, `<method> <path> <message>` exactly as
# oasdiff renders it (extra spaces collapsed), except that the method must be
# written LOWERCASE. oasdiff prints `GET` and matches either case, so an uppercase
# entry does suppress correctly — but
# scripts/__tests__/check-breaking-changes.test.sh requires every live entry to
# match `^(get|post|put|patch|delete) /` and fails on it, and that failure reads
# as a broken whitelist when the suppression is in fact fine. This note lives in
# the header rather than beside an entry because the per-entry version of it was
# pruned along with its entry, and the mistake immediately recurred. A line that
# matches nothing — including a `#` comment — is inert.
#
# Entries here are scoped to a `main`..PR diff, so an entry stops matching the
# moment its change lands on `main`. Prune dead entries when you notice them;
# an entry that outlives its diff suppresses nothing but reads as if it does.
# Prune ENTRIES, never the file: both the CI job and
# scripts/check-breaking-changes.sh pass this path unconditionally, and oasdiff
# exits 121 when it is missing. An entry-free file of comments is the floor.
#
# Entries must reproduce oasdiff's rendered message verbatim (a substring like
# "became nullable" matches nothing), so an oasdiff release that rewords a
# message silently un-matches its entry and turns the job red. If that happens,
# re-run the check locally and paste the new wording; don't assume the
# suppression is still in force because the line is still here.
#
# Every entry needs a written justification. "oasdiff is being annoying" is not
# one — a genuine breaking change gets a contract-version conversation, not a
# line in this file.
#
# Worked example of the pruning rule, kept because the rule reads abstractly
# until you have seen it applied once: the two wxyc-shared#297 suppressions
# (`BulkResolveTrackIdentity.track_position` and `BulkResolveResult.tracks`
# becoming nullable) landed on `main` in api.yaml 1.30.0, stopped appearing in
# any `main`..PR diff, and were pruned.
#
# Deliberately not recorded here: how many entries are currently live. A comment
# claiming a count is wrong the moment someone appends an entry below it, and no
# test can catch that — the checks in
# scripts/__tests__/check-breaking-changes.test.sh skip comment lines by
# construction. Read the entries.

# wxyc-shared#228 — the `Rotation` schema declared eleven properties non-nullable
# that `getRotationFromDB` has always been able to serve as null, and declared
# `add_date` as a `format: date` the wire cannot satisfy.
#
# Nullability. The query is `FROM rotation LEFT JOIN library / artists / format
# / genres`, and roughly 151 active rotation rows carry `album_id IS NULL` — an
# uncatalogued row has no `library` row to join, so every library-derived column
# on it is SQL NULL. Backend's own row type in `library.service.ts` declares all
# eleven `| null`; the contract said otherwise, which is the drift this PR
# exists to close. `artist_name`, `album_title` and `record_label` are
# additionally COALESCE fallbacks onto `rotation.*` snapshot columns that carry
# no NOT NULL of their own.
#
# add_date's format. `library.add_date` is a `timestamptz`, and this endpoint
# reads it through a raw `db.execute`, where drizzle's postgres-js driver
# installs a pass-through parser for the timestamp/date OIDs. The value reaches
# the client as Postgres' own rendering — `2026-08-20 00:00:00+00` — which is
# neither an RFC 3339 date-time nor a 10-byte full-date. The declared `date`
# format was therefore never true; removing it is a correction, not a widening.
# Backend#2349 tracks normalizing the server's serialization, which is where the
# fix belongs.
#
# Both are corrections of declarations that never described the wire, so no
# consumer can have been correctly relying on the shapes being removed. The
# contract takes a major, which is the signal consumers actually receive; these
# entries only let the diff land.
#
# PRUNE AFTER MERGE: diff-scoped, inert the moment this lands on main.
get /library/rotation the response property `items/id` became nullable for the status `200`
get /library/rotation the response property `items/code_letters` became nullable for the status `200`
get /library/rotation the response property `items/code_artist_number` became nullable for the status `200`
get /library/rotation the response property `items/code_number` became nullable for the status `200`
get /library/rotation the response property `items/artist_name` became nullable for the status `200`
get /library/rotation the response property `items/album_title` became nullable for the status `200`
get /library/rotation the response property `items/record_label` became nullable for the status `200`
get /library/rotation the response property `items/genre_name` became nullable for the status `200`
get /library/rotation the response property `items/format_name` became nullable for the status `200`
get /library/rotation the response property `items/plays` became nullable for the status `200`
get /library/rotation the response property `items/add_date` became nullable for the status `200`
get /library/rotation the `items/add_date` response's property `format` changed from `date` to `none` for status `200`
