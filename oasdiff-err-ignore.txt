# oasdiff error-level ignore list.
#
# Format: one line per suppressed finding, `<method> <path> <message>` exactly as
# oasdiff renders it (extra spaces collapsed), except that the method must be
# written LOWERCASE. oasdiff prints `GET` and matches either case, so an uppercase
# entry does suppress correctly — but
# scripts/__tests__/check-breaking-changes.test.sh requires every live entry to
# match `^(get|post|put|patch|delete) /` and fails on it, and that failure reads
# as a broken whitelist when the suppression is in fact fine. This note lives in
# the header rather than beside an entry because the per-entry version of it was
# pruned along with its entry, and the mistake immediately recurred. A line that
# matches nothing — including a `#` comment — is inert.
#
# Entries here are scoped to a `main`..PR diff, so an entry stops matching the
# moment its change lands on `main`. Prune dead entries when you notice them;
# an entry that outlives its diff suppresses nothing but reads as if it does.
# Prune ENTRIES, never the file: both the CI job and
# scripts/check-breaking-changes.sh pass this path unconditionally, and oasdiff
# exits 121 when it is missing. An entry-free file of comments is the floor.
#
# Entries must reproduce oasdiff's rendered message verbatim (a substring like
# "became nullable" matches nothing), so an oasdiff release that rewords a
# message silently un-matches its entry and turns the job red. If that happens,
# re-run the check locally and paste the new wording; don't assume the
# suppression is still in force because the line is still here.
#
# Every entry needs a written justification. "oasdiff is being annoying" is not
# one — a genuine breaking change gets a contract-version conversation, not a
# line in this file.
#
# Worked example of the pruning rule, kept because the rule reads abstractly
# until you have seen it applied once: the two wxyc-shared#297 suppressions
# (`BulkResolveTrackIdentity.track_position` and `BulkResolveResult.tracks`
# becoming nullable) landed on `main` in api.yaml 1.30.0, stopped appearing in
# any `main`..PR diff, and were pruned.
#
# Deliberately not recorded here: how many entries are currently live. A comment
# claiming a count is wrong the moment someone appends an entry below it, and no
# test can catch that — the checks in
# scripts/__tests__/check-breaking-changes.test.sh skip comment lines by
# construction. Read the entries.

# wxyc-shared#140 / wxyc-shared#127 — `AlbumSearchResult` declared `label` and
# `on_streaming` as non-nullable, and `LibraryCatalogItem` (reached from
# `POST /api/v1/lookup` via its `library_item` property) declared
# `on_streaming` the same way, even though `library.label` and
# `library.on_streaming` both carry no NOT NULL and `GET /library`'s
# producer (`serializeLibraryArtistViewEntry`) projects both columns
# straight through with no COALESCE. Both `on_streaming` declarations
# already said "Null if unknown" in their own description; the type just
# forbade the value the prose promised.
#
# The two `get /library/query` entries need one extra note, because that
# operation's mappers coalesce `label` to `''` and so cannot emit the null
# today. `AlbumSearchResult` is one schema shared by both operations and
# OpenAPI cannot narrow a shared schema per operation, so the declaration
# has to admit the wider of the two producers. `on_streaming` is raw on
# every producer, so it needs no such caveat.
#
# Response-side widening, so the contract takes a major (7.0.0 -> 8.0.0) —
# the same shape as the wxyc-shared#298 `RotationEntry.album_id` fix
# (6.0.0 -> 7.0.0), whose own suppressions were pruned from this file in
# af5b161 once they landed on `main`. `AlbumSearchResult` and
# `LibraryCatalogItem` are flat objects, not `allOf` branches, so oasdiff is
# certain rather than downgrading to a warning — unlike wxyc-shared#332 and
# wxyc-shared#496, whose properties sat inside an `allOf` branch and drew
# warnings for that reason alone.
#
# PRUNE AFTER MERGE: diff-scoped, inert the moment this lands on main.
post /api/v1/lookup the response property `results/items/library_item/on_streaming` became nullable for the status `200`
get /library the response property `items/label` became nullable for the status `200`
get /library the response property `items/on_streaming` became nullable for the status `200`
get /library/query the response property `results/items/label` became nullable for the status `200`
get /library/query the response property `results/items/on_streaming` became nullable for the status `200`

# wxyc-shared#140 — same PR, same argument, for the two fields sitting one and
# three lines from `label` in `AlbumSearchResult`: `library.label_id` and
# `library.album_artist` both carry no NOT NULL either
# (`shared/database/src/schema.ts`), `library_artist_view`'s latest definition
# (migration 0166) selects both straight off the base `library` FROM table
# with no COALESCE, and neither `/library/query` mapper
# (`taggedRowToAlbumSearchResultRow` / `toAlbumSearchResultRow`) nor
# `GET /library`'s serializer (`serializeLibraryArtistViewEntry`) coalesces
# either field — both pass the row value through unchanged on both
# operations, unlike `label`, which only `GET /library` leaves null.
#
# Neither field is in `AlbumSearchResult.required` (unlike `label`), so this
# widens nullability only — no required-set change, no version-bump-relevant
# shape decision beyond the same major bump `label`/`on_streaming` already
# took above.
#
# PRUNE AFTER MERGE: diff-scoped, inert the moment this lands on main.
get /library the response property `items/label_id` became nullable for the status `200`
get /library the response property `items/album_artist` became nullable for the status `200`
get /library/query the response property `results/items/label_id` became nullable for the status `200`
get /library/query the response property `results/items/album_artist` became nullable for the status `200`
